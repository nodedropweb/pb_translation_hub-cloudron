{
  "name": "pb-ckeditor-build",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted CKEditor 5 bundle for PB Translation Hub (DSGVO: no CDN). Pinned to v43.x — no licenseKey required, unified package = simple esbuild bundling.",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "build": "esbuild src/ckeditor.js --bundle --format=iife --outfile=../web/vendor/ckeditor/ckeditor.js --minify --legal-comments=none"
  },
  "dependencies": {
    "ckeditor5": "43.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
